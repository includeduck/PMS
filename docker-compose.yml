services:
  web:
    build: .
    ports:
      - "8000:8000"
    environment:
      DJANGO_DEBUG: "1"
      DJANGO_ALLOWED_HOSTS: "localhost,127.0.0.1"
      SQLITE_PATH: /app/data/db.sqlite3
    volumes:
      - db_data:/app/data
      - media_data:/app/media

volumes:
  db_data:
  media_data: